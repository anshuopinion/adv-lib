import React from "react";

import styled from "styled-components";

const StyledTeacher = styled.div``;

const Teacher = () => {
  return <StyledTeacher>This is Teacher page</StyledTeacher>;
};

export default Teacher;
